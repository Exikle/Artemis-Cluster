kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sonarr-pvc
  namespace: artemis-cluster
spec:
  storageClassName: appdata
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
