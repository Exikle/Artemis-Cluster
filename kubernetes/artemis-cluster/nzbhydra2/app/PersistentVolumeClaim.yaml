kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nzbhydra2-pvc
  namespace: artemis-cluster
spec:
  storageClassName: appdata
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
