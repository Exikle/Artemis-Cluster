apiVersion: v1
kind: Service
metadata:
  name: &app nzbhydra2
  namespace: artemis-cluster
  labels:
    app: *app
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 5076
  selector:
    app: *app
