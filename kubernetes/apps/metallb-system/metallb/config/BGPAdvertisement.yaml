apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: &pool external
  namespace: metallb-system
spec:
  ipAddressPools:
  - *pool
---
apiVersion: metallb.io/v1beta1
kind: BGPAdvertisement
metadata:
  name: &pool internal
  namespace: metallb-system
spec:
  ipAddressPools:
  - *pool