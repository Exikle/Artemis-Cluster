---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/pocketid.internal/pocketidoidcclient_v1alpha1.json
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: tekton-dashboard
spec:
  allowedUserGroups:
    - name: app-admin
      namespace: security
  callbackUrls:
    - "https://tekton.dcunha.io/oauth2/callback"
  clientID: tekton-dashboard
  launchUrl: "https://tekton.dcunha.io/"
  logo:
    autoGenerate: false
  logoutCallbackUrls:
    - "https://tekton.dcunha.io/"
  pkceEnabled: true
  secret:
    keys:
      clientSecret: client-secret
    storeClientSecret: true
