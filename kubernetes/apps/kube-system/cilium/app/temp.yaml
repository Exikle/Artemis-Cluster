apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: default-l2-policy
  namespace: kube-system
spec:
  loadBalancerIPs: true
  interfaces:
    - ^br0\.1099$ # Match your node interface
  nodeSelector:
    matchLabels:
      kubernetes.io/os: linux
