---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/provider_v1beta3.json
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: discord-bot
spec:
  type: discord
  channel: flux-cd
  secretRef:
    name: discord-webhook
