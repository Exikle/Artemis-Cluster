---
# Source: https://github.com/homeassistant-ai/ha-mcp
apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPServer
metadata:
  name: mcp-ha
spec:
  env:
    - name: HOMEASSISTANT_URL
      value: http://home-assistant.home-automation.svc.cluster.local:8123
  groupRef:
    name: artemis-ops
  image: ghcr.io/homeassistant-ai/ha-mcp:latest@sha256:a9d6892a0a44bbdfffe74a8bc060c15063bb296a5d0fd56a96bc8a9d530aaff4
  podTemplateSpec:
    spec:
      containers:
        - name: mcp
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            medium: Memory
  proxyPort: 8080
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      memory: 256Mi
  secrets:
    - name: ha-mcp
      key: HA_TOKEN
      targetEnvName: HOMEASSISTANT_TOKEN
  transport: stdio
