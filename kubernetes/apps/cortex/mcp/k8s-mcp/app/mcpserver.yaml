---
# Source: https://github.com/Flux159/mcp-server-kubernetes
# Auth: in-cluster ServiceAccount (no secrets needed — kubectl auto-detects SA at runtime)
# ClusterRole mcp-k8s grants read + patch/update on workloads + read on Flux CRDs
apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPServer
metadata:
  name: mcp-k8s
spec:
  groupRef:
    name: artemis-ops
  image: docker.io/flux159/mcp-server-kubernetes:v3.9.1@sha256:89592ffed4c49846edb798d9c96249719578756fcfac25e9cc1b12c77da69752
  proxyPort: 8080
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 512Mi
  transport: stdio
