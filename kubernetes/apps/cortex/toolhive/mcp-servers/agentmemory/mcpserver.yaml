---
# Source: https://github.com/rohitg00/agentmemory (@agentmemory/mcp)
# Connects to in-cluster agentmemory service — secret reused from agentmemory ExternalSecret
apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPServer
metadata:
  name: mcp-agentmemory
spec:
  groupRef:
    name: artemis-general
  image: node:24-alpine
  transport: stdio
  proxyPort: 8080
  env:
    - name: AGENTMEMORY_URL
      value: http://agentmemory.cortex.svc.cluster.local:3111
    - name: HOME
      value: /tmp
    - name: NPM_CONFIG_CACHE
      value: /tmp
  podTemplateSpec:
    spec:
      volumes:
        - name: tmp
          emptyDir: {}
      containers:
        - name: mcp
          command:
            - "npx"
            - "-y"
            - "@agentmemory/mcp"
          envFrom:
            - secretRef:
                name: agentmemory
          volumeMounts:
            - mountPath: /tmp
              name: tmp
  resources:
    limits:
      memory: 256Mi
    requests:
      cpu: 10m
      memory: 64Mi
