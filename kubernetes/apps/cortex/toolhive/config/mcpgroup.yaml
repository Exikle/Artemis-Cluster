---
# artemis-general — safe tools for all AI sessions
# Clients: Open WebUI (tools), Claude Code
# MCP servers: mcp-grafana (observability), mcp-searxng (web search), mcp-context7 (docs)
# External URL: https://mcp-general.dcunha.io (internal-gateway / LAN only)
apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPGroup
metadata:
  name: artemis-general
spec:
  description: "Safe tools for all AI sessions — search, observability. Clients: Open WebUI, Claude Code."

---
# artemis-media — media management tools
# Clients: Open WebUI (opt-in)
# MCP servers: mcp-arr (Sonarr/Radarr/Prowlarr), mcp-seerr (Jellyseerr)
# External URL: https://mcp-media.dcunha.io (internal-gateway / LAN only)
apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPGroup
metadata:
  name: artemis-media
spec:
  description: "Media management tools — arr stack, Jellyseerr. Clients: Open WebUI (opt-in)."

---
# artemis-ops — privileged infrastructure tools
# Clients: Claude Code only — do NOT wire to Open WebUI
# MCP servers: mcp-forgejo, mcp-github, mcp-ha, mcp-k8s
# External URL: https://mcp-ops.dcunha.io (internal-gateway / LAN only)
apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPGroup
metadata:
  name: artemis-ops
spec:
  description: "Privileged infra tools — Kubernetes, GitHub, Home Assistant. Clients: Claude Code only."
