---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &secretname immich
spec:
  dataFrom:
    - extract:
        key: immich
    - extract:
        key: tinyauth
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: *secretname
    template:
      engineVersion: v2
      data:
        DB_PASSWORD: "{{ .DB_PASSWORD }}"
        POSTGRES_PASSWORD: "{{ .DB_PASSWORD }}"
        immich.json: |
          {
            "oauth": {
              "enabled": true,
              "issuerUrl": "https://auth.dcunha.io",
              "clientId": "immich",
              "clientSecret": "{{ .IMMICH_CLIENT_SECRET }}",
              "scope": "openid profile email",
              "buttonText": "Login with Tinyauth",
              "autoRegister": false,
              "autoLaunch": true,
              "mobileOverrideEnabled": true,
              "mobileRedirectUri": "https://photos.dcunha.io/api/oauth/mobile-redirect"
            },
            "storageTemplate": {
              "enabled": true,
              "template": "{{ `{{y}}/{{MM}}/{{y}}-{{MM}}-{{dd}}/{{filename}}` }}"
            }
          }
