---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: autopulse
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: autopulse
    template:
      engineVersion: v2
      data:
        autopulse-username: "{{ .AUTOPULSE_USERNAME }}"
        autopulse-password: "{{ .AUTOPULSE_PASSWORD }}"
        jellyfin-token: "{{ .AUTOPULSE_JELLYFIN_TOKEN }}"
  dataFrom:
    - extract:
        key: autopulse
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: autopulse-ui
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: autopulse-ui
    template:
      engineVersion: v2
      data:
        FORCE_USERNAME: "{{ .AUTOPULSE_USERNAME }}"
        FORCE_PASSWORD: "{{ .AUTOPULSE_PASSWORD }}"
  dataFrom:
    - extract:
        key: autopulse
