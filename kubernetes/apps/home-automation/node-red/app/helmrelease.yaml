---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: node-red
spec:
  chartRef:
    kind: OCIRepository
    name: node-red
  interval: 1h
  values:
    controllers:
      node-red:
        containers:
          app:
            image:
              repository: nodered/node-red
              tag: 4.1.5-18-minimal@sha256:6f338396a8cf35e6f5fe2aef41b682b7f5dbc519ff2e28595d7c0cbd67577c50
            env:
              TZ: "America/Toronto"
              FLOWS: "flows.json"
              NODE_RED_ENABLE_PROJECTS: "true"
              NODE_RED_ENABLE_SAFE_MODE: "false"
              NODE_RED_PORT: &port 1880
              HA_URL: http://home-assistant.home-automation.svc.cluster.local:8123
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false # Node-RED writes to /data at runtime
              capabilities: {drop: ["ALL"]}
            resources:
              requests:
                memory: 350Mi
                cpu: 150m
              limits:
                memory: 1Gi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        ports:
          http:
            port: *port
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.dcunha.io"
        parentRefs:
          - name: internal-gateway
            namespace: network
    persistence:
      data:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /data
      tmp:
        type: emptyDir
