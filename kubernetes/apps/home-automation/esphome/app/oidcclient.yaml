---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/pocketid.internal/pocketidoidcclient_v1alpha1.json
apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: esphome
spec:
  allowedUserGroups:
    - name: app-admin
      namespace: security
    - name: app-ops
      namespace: security
  callbackUrls:
    - "https://esphome.dcunha.io/oauth2/callback"
  clientID: esphome
  launchUrl: "https://esphome.dcunha.io/"
  logo:
    autoGenerate: false
  logoutCallbackUrls:
    - "https://esphome.dcunha.io/"
  pkceEnabled: true
  secret:
    keys:
      clientSecret: client-secret
    storeClientSecret: true
