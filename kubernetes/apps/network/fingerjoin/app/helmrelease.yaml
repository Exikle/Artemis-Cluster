---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fingerjoin
spec:
  chartRef:
    kind: OCIRepository
    name: fingerjoin
  interval: 1h
  values:
    controllers:
      main:
        containers:
          main:
            image:
              repository: ghcr.io/exikle/fingerjoin
              tag: master@sha256:fe6dcc4c92f1478729d35bc8b40b9e01443ac2a9fd3ac7c951faa2887ea78145
            env:
              RUST_LOG: fingerjoin=info,tower_http=info
    route:
      app:
        annotations:
          gatus.home-operations.com/endpoint: |-
            conditions: ["[STATUS] == 400"]
        hostnames:
          - dcunha.io
        parentRefs:
          - name: external-gateway
            namespace: network
            sectionName: https
        rules:
          - matches:
              - path:
                  type: Exact
                  value: /.well-known/webfinger
            backendRefs:
              - identifier: main
                port: 80
