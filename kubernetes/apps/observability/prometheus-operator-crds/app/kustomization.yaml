---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  namespace: flux-system

resources:
  # Prometheus Operator CRDs - renovate: datasource=github-releases depName=prometheus-operator/prometheus-operator
  - https://github.com/prometheus-operator/prometheus-operator/releases/download/v0.78.2/stripped-down-crds.yaml

labels:
  - pairs:
      app.kubernetes.io/name: prometheus-operator-crds
      app.kubernetes.io/part-of: observability
