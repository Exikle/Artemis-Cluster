kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: plex-pvc
  namespace: default
spec:
  storageClassName: zfs-iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
