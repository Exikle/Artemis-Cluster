# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app minecraft
  namespace: flux-system
spec:
  targetNamespace: &ns artemis-cluster
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  dependsOn:
    - name: minecraft-redis
  path: ./kubernetes/apps/artemis-cluster/minecraft/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  postBuild:
    substitute:
      APP: *app
      SUBDOMAIN: *app
      ACCESS_PORT: "25565"
      NAMESPACE: *ns
      # VOLSYNC_CAPACITY: 15Gi
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app minecraft-redis
  namespace: flux-system
spec:
  targetNamespace: &ns artemis-cluster
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  dependsOn:
    - name: atlas-nfs
    - name: cloudnative-pg
  path: ./kubernetes/apps/artemis-cluster/minecraft/redis
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  postBuild:
    substitute:
      APP: *app
      SUBDOMAIN: *app
      ACCESS_PORT: "25565"
      NAMESPACE: *ns
