apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: &app sabnzbd
  namespace: artemis-cluster
  annotations:
    kubernetes.io/ingress.class: traefik-external
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`sabnzbd.dcunha.io`)
    kind: Rule
    services:
    - name: *app
      port: 8080
    middlewares:
    - name: default-headers
      namespace: kube-system
  tls:
    secretName: dcunha-io-tls
