---
apiVersion: v1
kind: Service
metadata:
  name: &app nginx
  namespace: &ns artemis-cluster
spec:
  selector:
    app: *app
  type: LoadBalancer
  ports:
    - name: https
      protocol: TCP
      targetPort: 8443
      port: 443
