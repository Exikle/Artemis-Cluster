---
apiVersion: v1
kind: Service
metadata:
  name: &app nginx
  namespace: &ns artemis-cluster
spec:
  type: LoadBalancer
  ports:
    - name: https
      protocol: TCP
      port: 443
  selector:
    app: *app
