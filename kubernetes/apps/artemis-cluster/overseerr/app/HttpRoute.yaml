---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: &app overseer
  namespace: &ns artemis-cluster
  annotations:
    external-dns.alpha.kubernetes.io/target: &host seer.dcunha.io
spec:
  hostnames:
    - *host
  parentRefs:
    - name: external-gateway
      namespace: default
      sectionName: https
  rules:
    - matches:
      - path:
          type: PathPrefix
          value: /
      backendRefs:
        - name: *app
          kind: Service
          namespace: *ns
          port: 5055