---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: "${APP}-internal"
  namespace: ${NAMESPACE:default}
  labels:
    app.kubernetes.io/name: ${APP}
    app.kubernetes.io/component: routing
spec:
  hostnames:
    - "${SUBDOMAIN}.dcunha.io"
  parentRefs:
    - name: internal-gateway
      namespace: default
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ${APP}
          kind: Service
          namespace: ${NAMESPACE:default}
          port: ${ACCESS_PORT}
          weight: 1
#---
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: <service-name>
#   namespace: <namespace>
#   annotations:
#     external-dns.alpha.kubernetes.io/internal: "true"
#     external-dns.alpha.kubernetes.io/hostname: "<service-name>.dcunha.lab"
# spec:
#   parentRefs:
#     - name: gateway
#       namespace: default
#       sectionName: http
#   hostnames:
#     - "<service-name>.dcunha.lab"
#   rules:
#     - backendRefs:
#         - name: <service-name>
#           port: <port>
