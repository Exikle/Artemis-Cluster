---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: "${APP}-external"
  namespace: ${NAMESPACE:default}
  labels:
    app.kubernetes.io/name: ${APP}
    app.kubernetes.io/component: routing
  annotations:
    external-dns.alpha.kubernetes.io/target: "${SUBDOMAIN}.dcunha.io"
spec:
  hostnames:
    - "${SUBDOMAIN}.dcunha.io"
  parentRefs:
    - name: external-gateway
      namespace: default
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ${APP}
          kind: Service
          namespace: ${NAMESPACE:default}
          port: ${ACCESS_PORT}
          weight: 1
# ---
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: myapp-external
#   namespace: myapp-namespace
#   annotations:
#     # External-DNS will create DNS record in Cloudflare
#     external-dns.alpha.kubernetes.io/hostname: "myapp.dcunha.io"
#     # Optional: Enable Cloudflare proxy (orange cloud)
#     external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
# spec:
#   parentRefs:
#     - name: gateway
#       namespace: default
#       sectionName: https-io  # Use the .io listener
#   hostnames:
#     - "myapp.dcunha.io"
#   rules:
#     - matches:
#         - path:
#             type: PathPrefix
#             value: /
#       backendRefs:
#         - name: myapp
#           port: 80
