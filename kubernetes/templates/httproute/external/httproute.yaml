---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: "${APP}-external"
  namespace: ${NAMESPACE:default}
  labels:
    app.kubernetes.io/name: ${APP}
    app.kubernetes.io/component: routing
  annotations:
    external-dns.alpha.kubernetes.io/target: "${SUBDOMAIN}.dcunha.io"
spec:
  hostnames:
    - "${SUBDOMAIN}.dcunha.io"
  parentRefs:
    - name: external-gateway
      namespace: default
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ${APP}
          kind: Service
          namespace: ${NAMESPACE:default}
          port: ${ACCESS_PORT}
          weight: 1
# ---
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: <service-name>
#   namespace: <namespace>
#   annotations:
#     external-dns.alpha.kubernetes.io/hostname: "<service-name>.dcunha.io"
#     external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
# spec:
#   parentRefs:
#     - name: gateway
#       namespace: default
#       sectionName: https-io
#   hostnames:
#     - "<service-name>.dcunha.io"
#   rules:
#     - backendRefs:
#         - name: <service-name>
#           port: <port>
