---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.networking.k8s.io/httproute_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: "${APP}-external"
  namespace: ${NAMESPACE:default}
  labels:
    app.kubernetes.io/name: ${APP}
    app.kubernetes.io/component: routing
  annotations:
    external-dns.alpha.kubernetes.io/target: "${SUBDOMAIN}.dcunha.io"
spec:
  hostnames:
    - "${SUBDOMAIN}.dcunha.io"
  parentRefs:
    - name: external-gateway
      namespace: default
      sectionName: https
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: ${APP}
          kind: Service
          namespace: ${NAMESPACE:default}
          port: ${ACCESS_PORT}
          weight: 1
# ---
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: my-external-app
#   namespace: my-namespace # Change this to the app's namespace
#   annotations:
#     # Defines the specific DNS record (e.g., app.dcunha.io)
#     external-dns.alpha.kubernetes.io/hostname: "my-app.dcunha.io"
#     # Note: No target needed, it inherits "ingress.dcunha.io" from Gateway
# spec:
#   parentRefs:
#     - name: external-gateway
#       namespace: default # <--- CRITICAL: Gateway is in 'default' namespace
#   hostnames:
#     - "my-app.dcunha.io"
#   rules:
#     - matches:
#         - path:
#             type: PathPrefix
#             value: /
#       backendRefs:
#         - name: my-app-service # Name of the Kubernetes Service
#           port: 80             # Port of the Kubernetes Service
