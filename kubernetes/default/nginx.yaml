apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nginx
  namespace: default
spec:
  interval: 2m
  chart:
    spec:
      chart: nginx
      version: '16.0.6'
      sourceRef:
        kind: HelmRepository
        name: nginx
        namespace: flux-system
      interval: 1m
  values:
    replicaCount: 3
    foo: value1
    bar:
    baz: value2
    oof:
    - item1
    - item2
