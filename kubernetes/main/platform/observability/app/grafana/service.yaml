apiVersion: v1
kind: Service
metadata:
  name: grafana # The name you want
  namespace: observability
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
