---
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: discord-bot
  namespace: flux-system
spec:
  type: discord
  channel: flux-cd
  secretRef:
    name: discord-webhook
