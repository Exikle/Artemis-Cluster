---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/cilium.io/ciliumloadbalancerippool_v2alpha1.json
apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: lb-ip-pool # Match your existing pool name
spec:
  blocks:
    # Existing range
    - start: "10.10.99.71"
      stop: "10.10.99.95"
    # Add Gateway range
    - start: "10.10.99.97"
      stop: "10.10.99.98"
  # No serviceSelector = available to all LoadBalancer services
