apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: minimal-test
  namespace: default
spec:
  parentRefs:
    - name: external
      namespace: default
  rules:
    - backendRefs:
        - name: kubernetes
          port: 443
