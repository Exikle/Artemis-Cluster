---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: flux-config
resources:
  - ./cluster.yaml
  - ./repositories.yaml
  - ../kustomizations/ # ← Reference the directory, not individual files
