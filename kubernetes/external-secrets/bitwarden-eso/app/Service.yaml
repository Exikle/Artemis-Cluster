apiVersion: v1
kind: Service
metadata:
  name: bitwarden-eso-bwsm-eso-provider
  namespace: external-secrets
  labels:
    # app.kubernetes.io/instance: bitwarden-eso
    app.kubernetes.io/name: bitwarden-eso
  annotations: {}
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  - port: 8443
    targetPort: https
    protocol: TCP
    name: https
  selector:
    app.kubernetes.io/name: bitwarden-eso
