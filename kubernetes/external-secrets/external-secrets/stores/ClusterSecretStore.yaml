apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: bw-cluster-store
  namespace: external-secrets
spec:
  provider:
    webhook:
      url: "http://bitwarden-eso-svc.external-secrets.local:8080/secret/{{ .remoteRef.key }}"
      headers:
        Content-Type: application/json
      result:
        jsonPath: "$.value"
