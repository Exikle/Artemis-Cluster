apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: bitwarden-secret-manager-eso
  namespace: external-secrets
spec:
  interval: 2m
  chart:
    spec:
      chart: external-secrets
      version: 0.0.8
      sourceRef:
        kind: HelmRepository
        name: external-secrets
        namespace: flux-system
      interval: 1m
  values:
    crds:
      installCRDs: false
