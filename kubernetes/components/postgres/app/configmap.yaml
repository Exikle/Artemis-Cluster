---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/core/configmap_v1.json
apiVersion: v1
kind: ConfigMap
metadata:
  name: ${PG_APP:=${APP}}-postgres
data:
  PGSSLMODE: verify-full
  PGSSLROOTCERT: /var/run/secrets/postgres-server-ca/ca.crt
