apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-settings
  namespace: flux-system
data:
  CLUSTER_NAME: artemis-cluster
  # site
  CLUSTER_TIME_ZONE: "America/Toronto"

  DOMAIN_NAME: "dcunha.io"
