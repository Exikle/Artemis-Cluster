#################################################################################################################
# Define the settings for the rook-ceph cluster with settings that should only be used in a test environment.
# A single filestore OSD will be created in the dataDirHostPath.
# For example, to create the cluster:
#   kubectl create -f common.yaml
#   kubectl create -f operator.yaml
#   kubectl create -f cluster-test.yaml
#################################################################################################################

apiVersion: ceph.rook.io/v1
kind: CephCluster
metadata:
  name: rook-ceph
  namespace: rook-ceph
spec:
  cephVersion:
    image: ceph/ceph:v14.2.5
    allowUnsupported: true
  dataDirHostPath: /var/lib/rook
  mon:
    count: 5
    allowMultiplePerNode: false
  resources:
    osd:
      requests:
        cpu: 200m
        memory: 1Gi
      limits:
        cpu: 5000m
        memory: 3Gi
  mgr:
    modules:
    - name: pg_autoscaler
      enabled: false
    - name: osd_pool_default_pg_num
      count: 1024
    - name: osd_pool_default_pgp_num
      count: 1024
    - name: diskprediction_local
      enabled: true
    - name: iostat
      enabled: true
  dashboard:
    enabled: true
    ssl: false
  network:
    hostNetwork: true
  rbdMirroring:
    workers: 0
  storage:
    useAllNodes: false
    nodes:
    - name: "prd002l"
      directories:
      - path: "/ceph"
      devices:
      # - name: "sda" boot device
      - name: "sdb"
      - name: "sdc"
      - name: "sdd"
      - name: "sde"
      - name: "sdf"
      - name: "sdg"
      - name: "sdh"
    - name: "pkn002l"
      directories:
      - path: "/ceph"
      devices:
      # - name: "sda" boot device
      - name: "sdb"
      - name: "sdc"
      - name: "sdd"
    - name: "pkn003l"
      directories:
      - path: "/ceph"
      devices:
      - name: "sda"
      - name: "sdb"
      # - name: "sdc" boot device
      - name: "sdd"
      - name: "sde"
      - name: "sdf"
    - name: "pkn004l"
      directories:
      - path: "/ceph"
      devices:
      # - name: "sda" boot device
      - name: "sdb"
      - name: "sdc"
    - name: "pkn005l"
      directories:
      - path: "/ceph"
      devices:
      - name: "sda"
      - name: "sdb"
      - name: "sdc"
      - name: "sdd"
      - name: "sde"
      - name: "sdf"
      # - name: "sdg" Currently backup for unraid
      - name: "sdh"
      - name: "sdi"
      - name: "sdj"
      - name: "sdk"
      - name: "sdl"
      - name: "sdm"
      - name: "sdn"
      - name: "sdo"
      - name: "sdp"
      # - name: "sdq" unraid usb, i wiped it by accident.....
      - name: "sdr"
      # - name: "sds" boot device
