kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nzbget-config
  namespace: default
spec:
  storageClassName:  ssd-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
