kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: plex-config-pvc
  namespace: default
spec:
  storageClassName:  ssd-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
