kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sonarr-config-pvc
  namespace: default
spec:
  storageClassName:  afs-ssd-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
