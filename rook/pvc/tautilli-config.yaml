kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: tautilli-config
  namespace: default
spec:
  storageClassName:  ssd-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
