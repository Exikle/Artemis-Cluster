kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: duskbot-config
  namespace: default
spec:
  storageClassName:  ssd-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
