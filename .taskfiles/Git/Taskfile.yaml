---
version: "3"

tasks:
  status:
    desc: Show git status
    cmds:
      - git status

  diff:
    desc: Show git diff
    cmds:
      - git diff

  push:
    desc: Push changes to origin
    cmds:
      - git push

  pull:
    desc: Pull latest changes
    cmds:
      - git pull --rebase
