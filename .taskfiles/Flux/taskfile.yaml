---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:

  bootstrap:
    desc: n/a
    cmds:
      - task: bootstrap-fluxcd

  bootstrap-fluxcd:
    desc: Install/Upgrade FluxCD
    cmd: brew install fluxcd/tap/flux
