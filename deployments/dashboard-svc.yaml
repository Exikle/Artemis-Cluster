---
apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard
  namespace: kubernetes-dashboard
  labels:
    app: kubernetes-dashboard
spec:
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 6789
  selector:
    app: kubernetes-dashboard
