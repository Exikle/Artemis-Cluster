kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sonarr-pvc
  namespace: default
spec:
  storageClassName: appdata
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
