kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: radarr-pvc
  namespace: default
spec:
  storageClassName: appdata
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
