apiVersion: v1
kind: Service
metadata:
  name: radarr-svc
  namespace: default
  labels:
    app: radarr-svc
spec:
  type: ClusterIP
  ports:
  - port: 7878
    protocol: TCP
  selector:
    app: radarr
