apiVersion: v1
kind: Service
metadata:
  name: overseerr-svc
  namespace: default
  labels:
    app: overseerr-svc
spec:
  type: ClusterIP
  ports:
  - protocol: TCP
    port: 5055
  selector:
    app: overseerr
