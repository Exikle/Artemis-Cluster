kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sabnzbd-pvc
  namespace: default
spec:
  storageClassName: appdata
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
